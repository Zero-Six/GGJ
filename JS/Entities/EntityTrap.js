class EntitySpikes extends Entity 
{
    constructor(scene, x, y)
    // {
        super(scene);
        //TODO: ajouter l'animation
        /*this.sprite1 = new PIXI.extras.AnimatedSprite();
        this.sprite1.x = x;
        this.sprite1.y = y;
        this.sprite1.animationSpeed = 0.3;
        this.sprite1.loop = false;
        this.sprite1.play();

        this.sprite2 = new PIXI.extras.AnimatedSprite();
        this.sprite2.x = x;
        this.sprite2.y = y;
        this.sprite2.animationSpeed = 0.3;
        this.sprite2.loop = false;
        this.sprite2.play();

        this.sprite1.onComplete = () => {
            this.destroy();
        }

        this.scene.addEntity(this);*/
    }
}